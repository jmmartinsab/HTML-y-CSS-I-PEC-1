{
  "name": "pec-1",
  "version": "1.0.0",
  "description": "This is boilerplate to create a web with html and css ",
  "scripts": {
    "parcel:serve": "parcel serve src/index.html -p 8123 --open",
    "parcel:build": "parcel build src/index.html --public-url ./ --dist-dir dist --no-source-maps --no-cache",
    "clean": "rimraf dist .cache .cache-loader .parcel-cache",
    "dev": "npm-run-all clean parcel:serve",
    "build": "npm-run-all clean parcel:build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jose Manuel Martín",
  "license": "MIT",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.0-nightly.636",
    "autoprefixer": "^10.2.3",
    "npm-run-all": "^4.1.5",
    "parcel": "^2.0.0-nightly.566",
    "postcss-preset-env": "^6.7.0",
    "rimraf": "^3.0.2",
    "sass": "^1.32.5"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^5.15.3",
    "hamburgers": "^1.1.3"
  }
}
